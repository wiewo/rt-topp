# several checks are disabled because various core templates or base classes throw these warnings
# cppcoreguidelines and hicpp are not very useful. They rather are recommendations than rules that should be enforced.
Checks: 'boost-*,bugprone-*,cert-*,mpi-*,openmp-*,clang-diagnostic-*,clang-analyzer-*,-clang-analyzer-alpha*,google-*,llvm-*,misc-*,readability-*,modernize-*,performance-*,-llvm-header-guard,-modernize-use-trailing-return-type,-readability-isolate-declaration,-readability-magic-numbers,-cert-err58-cpp'

# TODO: enable again when warnings are fixed
# WarningsAsErrors: '*'
HeaderFilterRegex: ''

# CheckOptions:

# design / refactoring (other parameters don't work well with macros)
# - key:    readability-function-size.ParameterThreshold
#   value:  5
